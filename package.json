{
  "name": "capinrs",
  "version": "1.0.0",
  "scripts": {
    "dev:typescript": "wrangler dev",
    "dev:wasm": "wrangler dev --env wasm",
    "deploy:typescript": "wrangler deploy",
    "deploy:wasm": "wrangler deploy --env wasm",
    "build:wasm": "cd wasm && wasm-pack build --target web --out-dir ../typescript/pkg"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}